package site.hsu.hub.application.adapter.in.web;import io.swagger.v3.oas.annotations.Operation;import jakarta.servlet.http.HttpServletRequest;import org.springframework.http.ResponseEntity;import site.hsu.hub.application.application.ApplicationService;import site.hsu.hub.common.api.ApiResponse;import java.util.List;interface OperatorApplicationControllerDocs{@Operation(summary="지원자 목록",description="운영 범위 모집의 지원자 목록만 반환하며 계정 이메일은 포함하지 않습니다.")ApiResponse<List<ApplicationService.ApplicationSummary>>list(Long id,HttpServletRequest req);@Operation(summary="지원서 상세",description="운영 범위를 재검증한 뒤 게시 당시 질문 의미와 답변을 반환합니다.")ApiResponse<ApplicationService.ApplicationDetail>detail(String id,HttpServletRequest req);@Operation(summary="이력서 PDF",description="운영 범위 확인 후 PDF를 inline·no-store·nosniff 보안 헤더로 반환합니다.")ResponseEntity<byte[]>resume(String id);}
