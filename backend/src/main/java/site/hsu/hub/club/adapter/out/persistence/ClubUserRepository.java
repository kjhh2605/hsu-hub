package site.hsu.hub.club.adapter.out.persistence;import org.springframework.data.jpa.repository.JpaRepository;import java.util.*;public interface ClubUserRepository extends JpaRepository<ClubUserEntity,Long>{boolean existsByUserIdAndClubIdAndRole(Long userId,Long clubId,site.hsu.hub.club.domain.ClubRole role);List<ClubUserEntity> findByUserIdAndRole(Long userId,site.hsu.hub.club.domain.ClubRole role);}
