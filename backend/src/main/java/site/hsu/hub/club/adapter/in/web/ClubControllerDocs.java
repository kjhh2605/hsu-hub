package site.hsu.hub.club.adapter.in.web;import io.swagger.v3.oas.annotations.Operation;import jakarta.servlet.http.HttpServletRequest;import org.springframework.http.ResponseEntity;import site.hsu.hub.common.api.ApiResponse;import site.hsu.hub.club.application.ClubService;import java.util.List;
interface ClubControllerDocs{@Operation(summary="동아리 목록",description="인증 사용자가 모든 동아리와 현재 모집 상태를 조회합니다.")ApiResponse<List<ClubService.ClubView>>list(HttpServletRequest req);@Operation(summary="동아리 상세",description="동아리 소개와 현재 모집 정보를 조회합니다.")ApiResponse<ClubService.ClubView>get(Long id,HttpServletRequest req);@Operation(summary="동아리 커버",description="비공개 저장소의 커버 이미지를 인증 후 바이너리로 반환합니다.")ResponseEntity<byte[]>cover(Long id);}
